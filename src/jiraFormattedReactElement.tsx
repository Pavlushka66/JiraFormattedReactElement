import * as React from 'react';

export default class JIraFormattedReactElement extends React.Component {
    render () {
        return (
          <div>
              Empty
          </div>
        );
      }
}